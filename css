:root{--beemo-avatar:url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><linearGradient id="screenGradient" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stop-color="%23A9E7C4"/><stop offset="100%" stop-color="%2391D3B0"/></linearGradient></defs><rect width="100" height="100" rx="18" ry="18" fill="%235EA287"/><rect x="10" y="4" width="80" height="60" rx="16" ry="16" fill="url(%23screenGradient)" stroke="%23394E43" stroke-width="3"/><path d="M42 45 Q50 47 58 45" stroke="%232D3B3B" stroke-width="6" fill="none" stroke-linecap="round"/><circle cx="33" cy="88" r="4" fill="%23EFB700"/><circle cx="57" cy="88" r="4" fill="%23C94B4B"/><circle cx="72" cy="76" r="3" fill="%2334C759"/><circle cx="25" cy="74" r="2.5" fill="%233A7ED9"/><circle cx="25" cy="82" r="2.5" fill="%233A7ED9"/></svg>');}

[data-testid="accounts-profile-button"] .rounded-full.h-6.w-6 {
  position:relative!important;
  width:24px!important; height:24px!important;
  border-radius:9999px!important;
  background-image:var(--beemo-avatar)!important;
  background-size:cover!important;
  background-position:center!important;
  background-repeat:no-repeat!important;
  background-color:transparent!important;
  overflow:hidden!important;
  transition:filter 0.3s ease-in-out;
  animation:beemoTiltBounce 14s ease-in-out infinite;
}

/* hide initials */
[data-testid="accounts-profile-button"] .rounded-full.h-6.w-6>* {
  opacity:0!important; pointer-events:none!important;
}

/* --- sparkle overlay --- */
[data-testid="accounts-profile-button"] .rounded-full.h-6.w-6::before {
  content:"";
  position:absolute; inset:0;
  background:linear-gradient(120deg, rgba(255,255,255,0) 40%, rgba(255,255,255,0.55) 50%, rgba(255,255,255,0) 60%);
  background-size:200% 100%;
  opacity:0;
  animation:beemoSparkle 12s ease-in-out infinite;
  border-radius:inherit;
  mix-blend-mode:screen;
  pointer-events:none;
}

/* --- both eyes as child pseudo elements --- */
[data-testid="accounts-profile-button"] .rounded-full.h-6.w-6::after {
  content:"";
  position:absolute;
  top:5px; left:6px;
  width:3px; height:3px;
  background:#2d3b3b;
  border-radius:50%;
  box-shadow: 9px 0 0 #2d3b3b; /* second eye to the right */
  transform-origin:center;
  animation:beemoBlink 7.5s ease-in-out infinite;
}

/* shimmer overlay — runs constantly, uses opacity only */
[data-testid="accounts-profile-button"] .rounded-full.h-6.w-6::before {
  content: "";
  position: absolute;
  inset: 0;
  border-radius: inherit;
  background: linear-gradient(
    120deg,
    rgba(255,255,255,0) 40%,
    rgba(255,255,255,0.45) 50%,
    rgba(255,255,255,0) 60%
  );
  background-size: 200% 100%;
  opacity: 0;
  animation: beemoSparkle 18s ease-in-out infinite;
  pointer-events: none;
  mix-blend-mode: screen;
}

/* smooth motion of sparkle */
@keyframes beemoSparkle {
  0%,80% { background-position: -200% 0; opacity: 0; }
  85%    { opacity: .9; }
  90%    { background-position: 200% 0; opacity: .6; }
  95%,100% { background-position: 200% 0; opacity: 0; }
}

/* hover glow layer — Chrome-safe (no filter animation) */
[data-testid="accounts-profile-button"] .rounded-full.h-6.w-6:hover {
  box-shadow:
    0 0 6px 2px #a9e7c4,
    0 0 12px 6px rgba(169,231,196,0.65),
    0 0 20px 10px rgba(169,231,196,0.35);
  filter: brightness(1.25);
  transition: box-shadow .25s ease, filter .25s ease;
}

/* ensure transitions apply both ways */
[data-testid="accounts-profile-button"] .rounded-full.h-6.w-6 {
  transition: box-shadow .25s ease, filter .25s ease;
}


/* --- animations --- */
@keyframes beemoShimmer {
  0%,100%{filter:brightness(1.1);}
  50%{filter:brightness(1.4) drop-shadow(0 0 6px #bfffe0);}
}
@keyframes beemoBlink {
  0%,88%,100%{transform:scaleY(1);}
  92%,96%{transform:scaleY(0.15);}
}
@keyframes beemoTiltBounce {
  0%,45%,100%{transform:rotate(0deg) translateY(0);}
  15%{transform:rotate(7deg) translateY(-2px);}
  20%,25%{transform:rotate(7deg) translateY(-2px);}
  65%{transform:rotate(-7deg) translateY(-2px);}
  70%,75%{transform:rotate(-7deg) translateY(-2px);}
}
